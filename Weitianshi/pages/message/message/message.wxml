<!--<import src="/pages/Template/Template.wxml" />
<block wx:for="{{messageList}}" wx:key="index">
  <template is="messageList" data="{{item:item}}"></template>
</block>-->
  <view class="beAddedContacts">
    <view class="messageList flex" bindtap="beAddedContacts">
      <view class="flex title">
        <image class="title_icon" src="/img/img-personMessage.png"></image>
        <view class="title">
          人脉申请
        </view>
      </view>
      <view class="flex number">
        <!--<label class="netWorkNum" wx:if="{{count !=0}}">{{count}}</label>-->
          <label class="netWorkNum" wx:if="{{count !=0}}">99</label>
        <label class="netWorkNumAfter" wx:if="{{count == 0}}"></label>
        <image class="iconEdit" src="/img/icon-edit@3x.png"></image>
      </view>
    </view>
  </view>